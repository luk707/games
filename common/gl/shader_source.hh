#pragma once

#include <string>

namespace Common
{
    namespace GL
    {
        struct ShaderSource
        {
            std::string VertexSource;
            std::string FragmentSource;
        };
    }
}
